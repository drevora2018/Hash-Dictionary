using System;
using System.Collections.Generic;
using System.Text;

namespace HashDictionary
{
    class City
    {
        internal string CityName, Population;

        public City(string City, string Pop)
        {
            CityName = City;
            Population = Pop;
        }
    }
}
